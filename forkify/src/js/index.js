/*
 string from "./models/Search"
// import {
//     add as a,
//     multiply as m,
//     ID as i
// } from "./views/searchView"
import * as searchView from "./views/searchView"
// console.log(`using imported functions : ${add(ID,2)} and ${multiply(5,6)}. and ${string}`);
// console.log(`using imported functions : ${a(i,2)} and ${m(5,6)}. and ${string}`);
console.log(`using imported functions : ${searchView.add(searchView.ID,2)} and ${searchView.multiply(5,6)}. and ${string}`);
*/